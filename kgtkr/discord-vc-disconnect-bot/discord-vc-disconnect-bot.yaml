apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: discord-vc-disconnect-bot
  name: discord-vc-disconnect-bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: discord-vc-disconnect-bot
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: discord-vc-disconnect-bot
    spec:
      containers:
        - image: ghcr.io/kgtkr/discord-vc-disconnect-bot:20260620100118033-fbe9638
          name: discord-vc-disconnect-bot
          resources:
            requests:
              cpu: "10m"
              memory: "15Mi"
          volumeMounts:
            - mountPath: /var/discord-vc-disconnect-bot/config
              name: discord-vc-disconnect-bot-config
              readOnly: true
          lifecycle:
            postStart:
              exec:
                command:
                  - ln
                  - -s
                  - /var/discord-vc-disconnect-bot/config/config.json
                  - /home/app/config.json
      restartPolicy: Always
      volumes:
        - name: discord-vc-disconnect-bot-config
          secret:
            secretName: discord-vc-disconnect-bot-config
